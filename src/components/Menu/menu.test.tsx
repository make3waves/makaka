import React from "react";
import { render } from "@testing-library/react";

import Menu, { MenuProps } from "./menu";
import MenuItem from "./menuItem";

describe('测试menu', () => {
  it('渲染带有默认属性的menu', () => {

  })

  it('选中menuItem，并触发用户定义的点击事件', () => {

  })

  it('渲染纵向排列的菜单', () => {

  })
})